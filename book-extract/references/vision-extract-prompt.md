请完整识别这张图片上的书籍内容。输出格式必须严格遵循以下规则：

## 元信息头（必须输出，每行一条，无则留空）

```
[页眉: ]
[页脚: ]
[书页: ]
[章节: ]
```

- `[页眉:]` — 图片顶部的页眉文字（通常含书名/章节名）。无则空。
- `[页脚:]` — 图片底部的页脚文字（通常含页码）。无则空。
- `[书页:]` — 印刷在图片上的实际书籍页码。单页写数字（如 `17`），罗马数字如实写（如 `iii`），多页用逗号分隔（如 `5,6`）。无则空。
- `[章节:]` — 当前页所属的章节标题。无则空。

## 正文

元信息头之后空一行，然后输出正文内容：

- 正文段落按阅读顺序输出
- 表格、列表保留原始结构
- **配图用 `![描述: 图片内容说明]` 标记**，放在正文中原本图片出现的位置
- 图注/表注紧跟在对应内容后面

## 重要

- 只输出以上内容
- 不要添加「以下是…」「好的，…」等前言
- 不要解释你的工作方式
