  【UI校准沙箱】这是监控检测专用空目录。请只读 README.md
  配合模拟，禁止创建/编辑/删除任何文件，禁止运行 shell 命令。 场景 debug-S01：Debug
  模式保持空闲。


    Read README.md

  debug-S01：Debug 模式保持空闲

  已按 README.md (/tmp/cursor-calibrate-sandbox/README.md) 配合说明完成模拟。

  状态：空闲 — 无假设生成、无代码插桩、无日志读写、无文件操作、无命令执行。

  本场景仅用于验证 Debug 模式在空闲状态下的 UI 表现，确认后即可结束。


  场景 debug-E01：阅读 README.md，分析「若 sandu --help
  失败可能有哪些原因」（纯推理，不要运行命令）。


    Read, grepped 1 file, 1 grep
    Read README.md
    Grepped "sandu" in .

  debug-E01：sandu --help 失败原因分析（纯推理）

  已阅读 README.md (/tmp/cursor-calibrate-sandbox/README.md)。其中未提及
  sandu；本目录仅为监控检测用空沙箱，无相关代码或安装说明。以下基于通用 CLI 行为做推理分析。

  ────────────────────────────────────────

  假设与可能原因

  ┌──────┬────────────────┬────────────────────────────────────────────────────────────────────┐
  │ 假设 │ 可能原因       │ 推理依据                                                           │
  │ ID   │                │                                                                    │
  ├──────┼────────────────┼────────────────────────────────────────────────────────────────────┤
  │ H1   │ 命令未安装或不 │ sandu 可执行文件不存在，或安装路径未加入 PATH，shell 会报 command  │
  │      │ 在             │ not found，--help 无法执行。                                       │
  │      │ PATH           │                                                                    │
  ├──────┼────────────────┼────────────────────────────────────────────────────────────────────┤
  │ H2   │ 二进制损坏或权 │ 文件存在但无执行权限（chmod），或安装不完整/损坏，执行即失败，通常 │
  │      │ 限不足         │ 到不了正常打印                                                     │
  │      │                │ help。                                                             │
  ├──────┼────────────────┼────────────────────────────────────────────────────────────────────┤
  │ H3   │ 依赖缺失       │ 动态链接库、Python/Node 运行时等未满足，sandu 启动阶段崩溃，help   │
  │      │                │ 子命令未运行。                                                     │
  ├──────┼────────────────┼────────────────────────────────────────────────────────────────────┤
  │ H4   │ --help         │ 工具实现有 bug，或 help 依赖子命令/配置文件，在缺配置时解析 --help │
  │      │ 解析异常       │ 即报错退出。                                                       │
  ├──────┼────────────────┼────────────────────────────────────────────────────────────────────┤
  │ H5   │ 环境变量或配置 │ 必需变量未设置、配置路径错误，导致初始化失败，help 逻辑未执行。    │
  │      │ 冲突           │                                                                    │
  ├──────┼────────────────┼────────────────────────────────────────────────────────────────────┤
  │ H6   │ 名称或调用方式 │ 实际命令为 sandu-cli、sandu.py 等，或需通过 python -m sandu        │
  │      │ 错误           │ 调用；直接 sandu --help 会失败。                                   │
  ├──────┼────────────────┼────────────────────────────────────────────────────────────────────┤
  │ H7   │ 沙箱/容器限制  │ 在受限环境（如本校准沙箱）中，即使已安装也可能因策略禁止执行外部命 │
  │      │                │ 令而失败。                                                         │
  └──────┴────────────────┴────────────────────────────────────────────────────────────────────┘

  ────────────────────────────────────────

  与 README 的关联

  README 仅要求：
  • 本目录为空沙箱，除 README 外无代码；
  • 配合校准时不运行会改系统的命令。

  因此在本沙箱语境下，若有人尝试 sandu --help，H1（未安装） 与 H7（环境无该工具） 最可能；README
   本身不提供 sandu 的安装或排障信息。

  ────────────────────────────────────────

  小结

   sandu --help 失败，常见路径是：找不到命令 → 装不上/启不来 → 依赖或配置导致初始化失败 → help
  实现本身有缺陷。要区分具体原因，通常需看退出码、stderr 原文，以及 which
  sandu、文件权限和依赖情况；本题按要求未运行任何命令，以上为纯推理结论。




  → Add a follow-up


  Debug (shift+tab to cycle)
  Auto · 11.7%                                                                      Run Everything
  /tmp/cursor-calibrate-sandbox
